/**
 * Registry.
 *
 * @packageDocumentation
 * @module Registry
 * @preferred
 */
import type { DidUri, IRegistry, IRegistryType, RegistryId, AuthorizationId, IRegistryAuthorizationDetails } from '@cord.network/types';
import { Option } from '@polkadot/types';
import type { PalletRegistryRegistryAuthorization } from '@cord.network/augment-api';
/**
 *  Checks whether the input meets all the required criteria of an [[IRegistry]] object.
 *  Throws on invalid input.
 *
 * @param input The potentially only partial [[IRegistry]].
 *
 */
export declare function verifyRegistryDataStructure(input: IRegistry): void;
/**
 * Calculates the registry Id by hashing it.
 *
 * @param registry  Registry for which to create the id.
 * @returns Registry id uri.
 */
export declare function getUriForRegistry(serializedRegistry: string, creator: DidUri): RegistryId;
/**
   * Creates a new [[Registry]] from an [[IRegistryType]].
 
   *
   * @param space The request from which the [[Registry]] should be generated.
   * @param controller The identity of the [[Registry]] creator.
   * @returns An instance of [[Registry]].
   */
export declare function fromRegistryProperties(registryProps: IRegistryType): IRegistry;
export declare function getAuthorizationIdentifier(registry: IRegistry['identifier'], authority: DidUri, creator: DidUri): AuthorizationId;
/**
 *  Custom Type Guard to determine input being of type ISpace using the SpaceUtils errorCheck.
 *
 * @param input The potentially only partial ISpace.
 * @returns Boolean whether input is of type ISpace.
 */
export declare function isIRegistry(input: unknown): input is IRegistry;
/**
 * Checks on the CORD blockchain whether a Registry is anchored.
 *
 * @param registry Registry data.
 */
export declare function verifyStored(registry: IRegistry): Promise<void>;
/**
 * Checks on the CORD blockchain whether a schema is registered.
 *
 * @param schema Schema data.
 */
export declare function verifyAuthorization(auth: AuthorizationId): Promise<void>;
/**
 * Checks on the CORD blockchain whether a Registry is anchored.
 *
 * @param auth authorization URI.
 */
export declare function fetchAuthorizationDetailsfromChain(auth: AuthorizationId): Promise<Option<PalletRegistryRegistryAuthorization>>;
export declare function getAuthorizationDetails(encodedEntry: Option<PalletRegistryRegistryAuthorization>): IRegistryAuthorizationDetails;
/**
 * Encodes the provided Schema for use in `api.tx.schema.add()`.
 *
 * @param schema The Schema to write on the blockchain.
 * @returns Encoded Schema.
 */
export declare function toChain(details: IRegistryType): string;
export declare function uriToIdentifier(registryId: IRegistry['identifier']): RegistryId;
